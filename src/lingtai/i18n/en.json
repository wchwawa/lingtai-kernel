{
  "read.description": "Read the contents of a text file. Returns numbered lines. Text files only — cannot read binary, images, or audio. Use offset/limit to read specific sections of large files.",
  "read.file_path": "Absolute path to the file to read",
  "read.offset": "Line number to start from (1-based)",
  "read.limit": "Max lines to read",
  "write.description": "Create or overwrite a file with the given content. Parent directories are created automatically. Use this for creating new files or complete rewrites. For small changes to existing files, prefer edit.",
  "write.file_path": "Absolute path to the file to write",
  "write.content": "Content to write",
  "edit.description": "Replace an exact string in a file. Fails if old_string is not found or is ambiguous.",
  "edit.file_path": "Absolute path to the file to edit",
  "edit.old_string": "The exact text to find and replace",
  "edit.new_string": "The replacement text",
  "edit.replace_all": "Replace all occurrences",
  "glob.description": "Find files matching a glob pattern. Use '**/' for recursive search (e.g. '**/*.py' finds all Python files). Returns sorted list of matching file paths.",
  "glob.pattern": "Glob pattern (e.g., '**/*.py')",
  "glob.path": "Directory to search in",
  "grep.description": "Search file contents for lines matching a regex pattern. Returns matching lines with file path and line number. Searches recursively when given a directory. Use the glob filter to narrow to specific file types.",
  "grep.pattern": "Regex pattern to search for",
  "grep.path": "File or directory to search in",
  "grep.glob": "File glob filter (e.g., '*.py')",
  "grep.max_matches": "Maximum matches to return",
  "bash.description": "Execute a shell command and return stdout/stderr. Any system program — scripts, git, curl, pip, data pipelines. Returns exit code, stdout, stderr. Supports async mode (async=true → job_id, then poll/cancel). See bash-manual skill for cron setup and advanced usage.",
  "bash.action": "Action to perform: 'run' (default) executes a command, 'poll' checks async job status, 'cancel' kills an async job",
  "bash.command": "The shell command to execute",
  "bash.timeout": "Timeout in seconds (default: 30, only for sync execution)",
  "bash.working_dir": "Working directory for the command (optional). Must be inside the agent working directory sandbox; paths outside it are rejected. For external repos/paths, keep working_dir at the agent dir and put an explicit cd in command, e.g. cd /absolute/path && ...",
  "bash.async": "Run command in background and return immediately with a job_id (default: false, only for action='run')",
  "bash.job_id": "Job ID for poll/cancel actions (returned by async run)",
  "codex.description": "Your durable journal across molts — what you've learned, decided, and discovered. Each entry's id + title + summary is visible in your system prompt; content and supplementary load on demand via view. Use progressive disclosure: keep summaries concise and scannable (1-3 sentences); put detailed explanations in content; raw data, citations, and long-form reference in supplementary. This layered approach keeps your prompt efficient while preserving depth. Slots are limited — consolidate related entries when you fill up. submit to add. view to read. consolidate to merge. delete to remove.",
  "codex.action": "submit: add a new entry (title + summary required; content + supplementary optional). view: read entry content by ID (requires ids; pass include_supplementary=true for backing material). consolidate: merge entries into one (requires ids, title, summary; content + supplementary optional). delete: remove entries (requires ids).",
  "codex.title": "Entry title — one line. Required for submit and consolidate.",
  "codex.summary": "Entry summary — 1-3 sentences. Always visible in your system prompt, so make it the part you'd want to see at a glance. Required for submit and consolidate.",
  "codex.content": "Main body — durable prose, written when you submit. Optional. Up to ~500 words is a good shape; longer goes in supplementary. Read on demand via view.",
  "codex.supplementary": "Backing material — citations, source data, the long form behind the principle. Optional for submit and consolidate. Read only when you call view with include_supplementary=true.",
  "codex.ids": "Entry IDs — for view, consolidate, and delete.",
  "codex.include_supplementary": "When true on view, also returns supplementary alongside content. Default: false.",
  "avatar.description": "Spawn a 他我 (alter ego) — a fully independent agent process. Inherits your init.json. For preset-equipped agents: boots on DEFAULT preset. Once spawned, it is detached — a new life, independent. Use mail or email to communicate. type='shallow' (default): blank slate, only init.json. type='deep': copies lingtai, pad, and codex. All spawns recorded in delegates/ledger.jsonl. IMPORTANT: the standard 'reasoning' parameter you write on this tool call automatically becomes the avatar's starting prompt — so write it as a thorough mission briefing, not just a rationale for the call. Actions: 'spawn': create new 他我. 'rules': set network-wide rules (requires karma). WARNING: each spawn creates an independent process consuming resources until sleep'd or suspended. Double-check reasoning before invoking. Use dry_run=true to preview.",
  "avatar.name": "True name (真名) for the 他我 (required, e.g. 'researcher', 'analyst', '学者'). This doubles as the avatar's working-directory basename under .lingtai/, so it must be a bare single segment: letters (any script — Latin, CJK, Cyrillic, etc.), digits, underscore, or hyphen only. No slashes, no dots, no spaces, no leading '.', max 64 chars. The avatar's display name (nickname) can be set separately and has no such constraints.",
  "avatar.type": "Spawn type. 'shallow' (default, 初生): blank slate — only init.json (LLM config + capabilities) inherited. 'deep' (二重身): full copy of character, pad, and codex.",
  "avatar.comment": "A persistent system-level note injected into the avatar's system prompt (rendered last, after memory). This is NOT inherited from parent — it defaults to empty. Use this ONLY for instructions the avatar must ALWAYS remember across all molts and context resets — e.g. critical constraints, environment setup notes, or safety rules. This survives everything: molt, refresh, sleep/wake. Leave empty unless you have something the avatar should never forget.",
  "avatar.action": "Action to perform. 'spawn' (default): create a new 他我. 'rules': set network rules — writes .rules to your directory and distributes to all descendants in the avatar tree. Requires admin privilege (karma). Rules are injected into the system prompt (after covenant, before tools) and persist across molts.",
  "avatar.rules_content": "Rules content (required for action='rules'). Plain text — one rule per line. These are non-negotiable constraints distributed to all your descendants. Example: 'Always report findings via email.\\nDo not spawn more than 3 avatars.'",
  "avatar.dry_run": "If true, return a preview of the spawn (name, type, working_dir, mission) without creating the process or touching the filesystem. Use this to sanity-check a spawn — especially the mission you would deliver — before committing.",
  "avatar.confirm": "Acknowledgement that you have double-checked the mission (the reasoning field) and intend to spawn an independent process. Required when the mission looks empty / very short / test-like; otherwise optional. Setting confirm=true bypasses the mission-quality gate but does NOT bypass dry_run.",
  "avatar.parent_prompt": "[system] You are an avatar of {parent_name}, whose address is {parent_address}. Please keep this in your psyche memory so you remember who spawned you. When you complete your mission, encounter problems you cannot resolve, or need to report back, email your parent at the address above.",
  "vision.description": "Analyze an image using the LLM's vision capabilities. Supports JPEG, PNG, and WebP. Ask any question about the image — describe contents, read text, interpret charts, identify objects, assess style or mood. Combine with draw to generate then analyze images.",
  "vision.image_path": "Path to the image file",
  "vision.question": "Question about the image",
  "web_search.description": "Search the web for current information. Use for real-time data, recent events, documentation, or anything beyond your training knowledge. Returns ranked search results with titles, URLs, and snippets. To fetch and read a specific URL, or for deeper strategies (PDF download, JS-rendered pages, scraping with stealth, fallback APIs), see the `web-browsing` skill.",
  "web_search.query": "Search query",
  "daemon.description": "Daemon (神識) — delegate work to ephemeral subagents for context isolation. Each is a disposable LLM session sharing your working directory, retaining no memory after completion. Use for noisy work where you only need the conclusion. Results truncated to ~2000 chars — instruct the emanation to write detailed output to a file. Actions: emanate (dispatch), list (status), ask (follow-up), check (inspect recent events), reclaim (kill all). See daemon-manual skill for inspection patterns.",
  "daemon.action": "Action to perform: 'emanate' (dispatch subagents), 'list' (show status), 'ask' (follow-up message), 'check' (read recent events of one emanation), 'reclaim' (kill all)",
  "daemon.last": "For 'check': how many most-recent events to return from events.jsonl. Default 20. Each turn produces ~2 events (tool_call + tool_result), so last=20 covers ~10 tool dispatches.",
  "daemon.truncate": "For 'check': maximum length of any string field in returned events; longer fields get an ellipsis suffix. Default 500. Set to 0 to disable.",
  "daemon.max_turns": "For 'emanate': max LLM tool-loop turns per emanation. Default: parent ceiling (200). Use a smaller value to keep simple emanations bounded.",
  "daemon.timeout": "For 'emanate': max wall-clock seconds for the whole batch. Default: parent max (3600s). Watchdog kills any emanation still running when the timer fires.",
  "daemon.tasks": "List of task objects for 'emanate'. Each: {task: str (required — instructions including where to save work), tools: list[str] (required — capability names, e.g. ['file', 'bash']), preset: str (optional — preset file path, use name from system(action='presets') output). Omit preset to inherit the parent's tool surface. MCP tools are always inherited. See daemon-manual for preset inheritance and capability resolution details.",
  "daemon.tasks.preset": "Optional preset file path. Must be a .json/.jsonc path as returned by system(action='presets'). Do NOT use shorthand names — use the full 'name' field from the presets listing. Example: '~/.lingtai-tui/presets/saved/cheap.json'. Omit to inherit the parent's tool surface.",
  "daemon.id": "Subagent ID for 'ask' action (e.g. 'em-1')",
  "daemon.message": "Follow-up message for 'ask' action",
  "library.description": "Your per-agent skill library. The <available_skills> catalog in your system prompt lists every skill reachable right now. Call info to read the full library workflow (authoring, publishing, loading via pad.append) and verify your library is healthy. See also the library-manual skill in .library/intrinsic/.",
  "library.action_info": "info: return the library-manual skill body plus a runtime health snapshot (catalog size, resolved paths, problems).",
  "library.preamble": "The following skills provide specialized instructions for specific tasks. When a task matches a skill's description, use the read tool to load the SKILL.md file at the path shown in that skill's location field, then follow its instructions. Each skill folder may contain supporting files (scripts, templates, data) — the SKILL.md will reference them. Skills you use often can be pinned into your pad via psyche({object:'pad', action:'append', files:[location]}) for cross-turn persistence. If no skill clearly applies, do not load any."
}